class Solution {
public:
    string largestOddNumber(string arr) {
        int n=arr.size();
        for(int i=n-1;i>=0;i--){
            if((arr[i]-'0')%2==1){
                return arr.substr(0,i+1);
            }
        }
        return "";
    }
};

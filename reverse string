class Solution {
public:
    void reverseString(vector<char>& s) {
        int n=s.size();
        int m=0;
        int p=n-1;
        while(m<p){
            swap(s[m++],s[p--]);
        }
        
    }
};
